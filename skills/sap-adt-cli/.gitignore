# Credentials - NEVER commit
.env
.env.disabled
*.env.local

# Runtime output and Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Real-machine raw ADT captures — internal hosts, usernames and business data.
# Sanitized copies live in tests/fixtures/; see tests/fixtures/README.md.
tests/fixtures/raw/
